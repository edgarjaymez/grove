test-results
node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
/dist

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

*storybook.log
storybook-static
.env*.local

# Agent memory — never commit (holds personal/session context). Grove's lives in
# ~/.claude/projects/.../memory/ (outside the repo); these guards cover forks/in-tree setups.
MEMORY.md
.claude/**/memory/
